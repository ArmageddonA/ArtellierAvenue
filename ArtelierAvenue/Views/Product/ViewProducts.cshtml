@model IEnumerable<ArtelierAvenue.Models.Product>
@{
        ViewBag.Title = "View Products";
}
<p>
    @Html.ActionLink("Create New", "AddorEdit", new { id = 0})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ComissionPrice)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductType.ProductTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vendor.VendorName)
        </th>
        <th>Stock Amounts</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.ProductName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ComissionPrice)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Price.Amount)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ProductStatu.ProductStatusName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ProductType.ProductTypeName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Vendor.VendorName)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Vendor.VendorName)
    </td>
    <td>
        @Html.ActionLink("Edit", "AddorEdit", new { id = item.ProductID }) |
        @Html.ActionLink("Details", "Details", new { id = item.ProductID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.ProductID })
    </td>
</tr>
}

</table>
